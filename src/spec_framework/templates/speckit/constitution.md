You are performing Phase 1 of Spec-Driven Development: CONSTITUTION.

Your goal is to establish the foundational principles, constraints, and non-negotiables
for the project. This document guides every future architectural and implementation
decision — treat it as the single source of truth.

## Steps

1. If the user has provided context below, use it. Otherwise ask 2–3 focused
   clarifying questions before proceeding.
2. Produce a `CONSTITUTION.md` covering:
   - **Vision** — one sentence describing the project's purpose and primary user.
   - **Principles** — 3–7 guiding architectural/design principles (e.g. "offline-first",
     "zero external dependencies", "immutable data model").
   - **Constraints** — hard limits: language/runtime, supported platforms, legal,
     security requirements, performance budgets.
   - **Non-goals** — what this project explicitly will NOT do (prevents scope creep).
   - **Success criteria** — how "done" is defined for the initial release.
3. Save the document to `docs/spec/CONSTITUTION.md` (create the directory if needed).
4. End your reply with a one-paragraph summary of the key decisions and any open
   questions that should be resolved before moving to Phase 2 (`/speckit.specify`).

## User context
